Name: Python CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pytest
        if [ -f bot-manager/bot-engine/requirements.txt ]; then pip install -r bot-manager/bot-engine/requirements.txt; fi
        if [ -f api-fundacion-idear-webhook/requirements.txt ]; then pip install -r api-fundacion-idear-webhook/requirements.txt; fi
    - name: Lint with flake8
      run: |
        # Detener la ejecución si hay errores de linter (Ignorando la carpeta venv)
        flake8 . --count --exclude=venv,.venv,env --select=E9,F63,F7,F82 --show-source --statistics
        # Advertir si hay errores de estilo (Ignorando la carpeta venv)
        flake8 . --count --exclude=venv,.venv,env --exit-zero --max-complexity=10 --max-line-length=120 --statistics
    - name: Test with pytest
      run: |
        # Si tienes tests, configúralos aquí. Por ahora, solo es un placeholder.
        echo "No hay tests configurados aún. ¡Este es un buen lugar para añadir los tuyos!"
        # pytest
        
